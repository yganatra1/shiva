# Repository configuration

Shiva V0.1 is configured through environment variables; see `.env.example` at the repository root. This directory is reserved for future non-secret, version-controlled configuration.

Never place credentials or production secrets here. RunPod runtime configuration remains outside Git and is provisioned separately.
