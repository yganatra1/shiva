# Scripts

This directory is reserved for small, reviewed development and deployment helpers. V0.1 does not require repository-specific shell scripts; the supported commands live in `app/package.json`.
