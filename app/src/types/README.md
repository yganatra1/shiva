# Shared types

This directory is reserved for cross-cutting domain types once Shiva grows beyond the V0.1 provider and chat contracts. Types should remain close to their owning feature until they are genuinely shared.
